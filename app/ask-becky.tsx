'use client';
import { FormEvent, useState } from 'react';
export default function AskBecky(){const[question,setQuestion]=useState('');const[answer,setAnswer]=useState('');const[loading,setLoading]=useState(false);async function ask(event:FormEvent){event.preventDefault();if(!question.trim())return;setLoading(true);setAnswer('');try{const response=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question})});const result=await response.json();if(response.status===401)setAnswer('Please sign in to ask Becky.');else setAnswer(result.answer||result.error||'Becky could not answer that yet.')}catch{setAnswer('Becky could not connect. Please try again.')}finally{setLoading(false)}}return <><form className="ask-box" onSubmit={ask}><span className="sparkle" aria-hidden="true">✦</span><label className="sr-only" htmlFor="question">Ask a question</label><input id="question" value={question} onChange={e=>setQuestion(e.target.value)} placeholder="How do I start the heating?"/><button type="submit" disabled={loading}>{loading?'Thinking…':'Ask Becky'} <span>→</span></button></form>{answer&&<div className="ask-answer" role="status">{answer}{answer.startsWith('Please sign in')&&<> <a href="/login">Sign in</a></>}</div>}<p className="try-it">Try asking: <button onClick={()=>setQuestion('Where is the fuel shut-off?')}>Where is the fuel shut-off?</button> <i>·</i> <button onClick={()=>setQuestion('What is the best pub nearby?')}>Best pub nearby?</button></p></>}
