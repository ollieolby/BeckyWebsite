'use client';
import { FormEvent,useState } from 'react';
type Asset={id:string;name:string;slug:string};
export default function AdminForms({assets,canEdit}:{assets:Asset[];canEdit:boolean}){const[status,setStatus]=useState('');async function submit(url:string,body:BodyInit,headers?:HeadersInit){setStatus('Saving…');const response=await fetch(url,{method:'POST',body,headers});const result=await response.json();setStatus(response.ok?'Saved.':result.error||'Unable to save.')}if(!canEdit)return <div className="notice"><strong>You have view-only access.</strong><p>An administrator must promote your profile to editor before you can add content.</p></div>;return <><div className="admin-tabs"><article className="admin-panel"><h2>Upload a manual</h2><p>PDF, text, or Markdown; up to 50 MB.</p><form onSubmit={(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();submit('/api/documents',new FormData(e.currentTarget))}}><label>Title<input name="title" required placeholder="Webasto heating manual"/></label><label>For<select name="asset_id"><option value="">General</option>{assets.map(a=><option value={a.id} key={a.id}>{a.name}</option>)}</select></label><label>Document<input name="file" type="file" accept=".pdf,.txt,.md" required/></label><button>Upload and index</button></form></article><article className="admin-panel"><h2>Save a place</h2><p>Add a mooring, pub, café, shop, or fuel stop.</p><form onSubmit={(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();submit('/api/places',JSON.stringify(Object.fromEntries(new FormData(e.currentTarget))),{'Content-Type':'application/json'})}}><label>Name<input name="name" required placeholder="The Riverside Arms"/></label><label>Category<select name="category"><option value="mooring">Mooring</option><option value="pub">Pub</option><option value="cafe">Café</option><option value="shop">Shop</option><option value="fuel">Fuel</option><option value="other">Other</option></select></label><div className="form-row"><label>Latitude<input name="latitude" type="number" step="any" required/></label><label>Longitude<input name="longitude" type="number" step="any" required/></label></div><label>Notes<textarea name="notes" rows={4}/></label><button>Save place</button></form></article></div><p className="save-status" role="status">{status}</p></>}
